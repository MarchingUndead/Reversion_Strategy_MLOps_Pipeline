# Python environment spec consumed by MLproject.
# MLflow uses this to create a virtualenv for `mlflow run .` unless --env-manager=local is passed.
python: "3.10"

build_dependencies:
  - pip
  - setuptools
  - wheel

dependencies:
  - -r requirements.txt
